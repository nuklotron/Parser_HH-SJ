CREATE TABLE vacancies
(
    title varchar(100) NOT NULL,
    description text,
    salary_from int,
    salary_to int,
    city varchar(100),
    url text,
    employer varchar(100),
    emp_id int
);
